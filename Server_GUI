import subprocess
import time



process = subprocess.Popen(["server.sh"])

print("Server started")
time.sleep(10)  # do other stuff while server runs

process.terminate()
process.wait()
print("Server stopped")
